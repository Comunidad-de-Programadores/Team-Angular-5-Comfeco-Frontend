import { FrameworkChipColorPipe } from './framework-chip-color.pipe';

describe('FrameworkChipColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FrameworkChipColorPipe();
    expect(pipe).toBeTruthy();
  });
});
